<!DOCTYPE html>
<html lang="fr">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>{% block title %}TerrainTrack - Gestion d'interventions et de flotte{% endblock %}</title>
    
    <!-- TailwindCSS via CDN -->
    <script src="https://cdn.tailwindcss.com"></script>
    
    <!-- Boxicons -->
    <link href="https://cdn.jsdelivr.net/npm/boxicons@2.1.4/css/boxicons.min.css" rel="stylesheet">
    
    {% block extra_head %}{% endblock %}
    
    <style>
        * {
            margin: 0;
            padding: 0;
            box-sizing: border-box;
        }
        
        body {
            font-family: -apple-system, BlinkMacSystemFont, 'Segoe UI', 'Roboto', 'Oxygen', 'Ubuntu', 'Cantarell', 'Fira Sans', 'Droid Sans', 'Helvetica Neue', sans-serif;
            -webkit-font-smoothing: antialiased;
            -moz-osx-font-smoothing: grayscale;
        }
        
        .header-modern {
            background: rgba(255, 255, 255, 0.98);
            backdrop-filter: blur(10px);
            box-shadow: 0 2px 20px rgba(0, 0, 0, 0.08);
            border-bottom: 1px solid rgba(226, 232, 240, 0.8);
            position: sticky;
            top: 0;
            z-index: 1000;
            transition: all 0.3s ease;
        }
        
        .logo-link {
            display: flex;
            align-items: center;
            gap: 12px;
            text-decoration: none;
            font-size: 1.5rem;
            font-weight: 800;
            color: #1e40af;
            transition: transform 0.2s ease;
        }
        
        .logo-link:hover {
            transform: translateY(-2px);
        }
        
        .logo-link i {
            background: linear-gradient(135deg, #2563eb, #1d4ed8);
            -webkit-background-clip: text;
            -webkit-text-fill-color: transparent;
            background-clip: text;
            filter: drop-shadow(0 2px 4px rgba(37, 99, 235, 0.3));
        }
        
        .nav-link {
            color: #64748b;
            text-decoration: none;
            font-weight: 600;
            font-size: 0.95rem;
            position: relative;
            padding: 0.5rem 0;
            transition: all 0.3s ease;
        }
        
        .nav-link::after {
            content: '';
            position: absolute;
            bottom: 0;
            left: 0;
            width: 0;
            height: 2px;
            background: linear-gradient(90deg, #2563eb, #1d4ed8);
            transition: width 0.3s ease;
        }
        
        .nav-link:hover {
            color: #2563eb;
        }
        
        .nav-link:hover::after,
        .nav-link.active::after {
            width: 100%;
        }
        
        .nav-link.active {
            color: #2563eb;
        }
        
        .btn-primary {
            background: linear-gradient(135deg, #2563eb, #1d4ed8);
            color: white;
            padding: 0.625rem 1.5rem;
            border-radius: 8px;
            text-decoration: none;
            font-weight: 600;
            font-size: 0.9rem;
            transition: all 0.3s ease;
            box-shadow: 0 4px 12px rgba(37, 99, 235, 0.3);
            border: none;
            cursor: pointer;
            display: inline-flex;
            align-items: center;
            gap: 0.5rem;
        }
        
        .btn-primary:hover {
            transform: translateY(-2px);
            box-shadow: 0 6px 20px rgba(37, 99, 235, 0.4);
        }
        
        .btn-secondary {
            color: #2563eb;
            padding: 0.625rem 1.5rem;
            border-radius: 8px;
            text-decoration: none;
            font-weight: 600;
            font-size: 0.9rem;
            transition: all 0.3s ease;
            border: 2px solid transparent;
        }
        
        .btn-secondary:hover {
            color: #1d4ed8;
            background: rgba(37, 99, 235, 0.05);
        }
        
        .footer-modern {
            background: linear-gradient(180deg, #0f172a 0%, #1e293b 100%);
            color: #cbd5e1;
            padding: 4rem 0 2rem;
            margin-top: 6rem;
            border-top: 1px solid rgba(255, 255, 255, 0.1);
        }
        
        .footer-link {
            color: #94a3b8;
            text-decoration: none;
            font-size: 0.875rem;
            transition: all 0.3s ease;
            display: inline-block;
        }
        
        .footer-link:hover {
            color: #60a5fa;
            transform: translateX(4px);
        }
    </style>
</head>
<body>
    <!-- Header -->
    <header class="header-modern">
        <div style="max-width: 1280px; margin: 0 auto; padding: 0 32px; display: flex; align-items: center; justify-content: space-between; height: 80px;">
            <a href="/exemple/backend-mvc/public/" class="logo-link">
                <i class='bx bxs-truck' style="font-size: 2rem;"></i>
                <span style="background: linear-gradient(135deg, #2563eb, #1d4ed8); -webkit-background-clip: text; -webkit-text-fill-color: transparent; background-clip: text;">TerrainTrack</span>
            </a>
            
            <nav style="display: flex; align-items: center; gap: 40px;">
                <a href="/exemple/backend-mvc/public/" class="nav-link" id="nav-home">Accueil</a>
                <a href="/exemple/backend-mvc/public/pricing" class="nav-link" id="nav-pricing">Tarifs</a>
                <a href="/exemple/backend-mvc/public/support" class="nav-link" id="nav-support">Support</a>
            </nav>
            
            <div style="display: flex; align-items: center; gap: 16px;">
                {% if user %}
                    <a href="/exemple/backend-mvc/public/dashboard" class="btn-secondary">Dashboard</a>
                    <a href="/exemple/backend-mvc/public/auth/logout" class="btn-primary">Déconnexion</a>
                {% else %}
                    <a href="/exemple/backend-mvc/public/login" class="btn-secondary">Connexion</a>
                    <a href="/exemple/backend-mvc/public/register" class="btn-primary">Inscription</a>
                {% endif %}
            </div>
        </div>
    </header>

    <!-- Main Content -->
    <main>
        {% block content %}{% endblock %}
    </main>

    <!-- Footer -->
    <footer class="footer-modern">
        <div style="max-width: 1280px; margin: 0 auto; padding: 0 32px;">
            <div style="display: grid; grid-template-columns: 2fr 1fr; gap: 4rem; margin-bottom: 3rem; padding-bottom: 3rem; border-bottom: 1px solid rgba(255, 255, 255, 0.1);">
                <div>
                    <h3 style="display: flex; align-items: center; gap: 12px; margin-bottom: 1.5rem; color: white; font-size: 1.5rem; font-weight: 700;">
                        <i class='bx bxs-truck' style="font-size: 1.75rem; background: linear-gradient(135deg, #60a5fa, #3b82f6); -webkit-background-clip: text; -webkit-text-fill-color: transparent; background-clip: text;"></i>
                        <span style="background: linear-gradient(135deg, #ffffff, #e2e8f0); -webkit-background-clip: text; -webkit-text-fill-color: transparent; background-clip: text;">TerrainTrack</span>
                    </h3>
                    <p style="font-size: 0.9375rem; line-height: 1.8; color: #94a3b8; max-width: 400px;">
                        Solution SaaS complète pour la gestion d'interventions et de flotte de véhicules. Simplifiez vos opérations et optimisez votre productivité.
                    </p>
                </div>
                
                <div>
                    <h4 style="color: white; margin-bottom: 1.25rem; font-weight: 700; font-size: 1.1rem; letter-spacing: 0.5px;">Légal</h4>
                    <ul style="list-style: none; padding: 0; display: flex; flex-direction: column; gap: 0.75rem;">
                        <li><a href="/exemple/backend-mvc/public/mentions-legales" class="footer-link">Mentions légales</a></li>
                        <li><a href="/exemple/backend-mvc/public/politique-confidentialite" class="footer-link">Politique de confidentialité</a></li>
                        <li><a href="/exemple/backend-mvc/public/cgu" class="footer-link">CGU</a></li>
                    </ul>
                </div>
            </div>
            
            <div style="text-align: center; padding-top: 2rem;">
                <p style="font-size: 0.875rem; color: #64748b; letter-spacing: 0.3px;">
                    &copy; {{ "now"|date("Y") }} <strong style="color: #94a3b8;">TerrainTrack</strong>. Tous droits réservés.
                </p>
            </div>
        </div>
    </footer>

    <script>
        // Marquer le lien actif
        const currentPath = window.location.pathname;
        const navLinks = {
            '/': 'nav-home',
            '/exemple/backend-mvc/public/': 'nav-home',
            '/pricing': 'nav-pricing',
            '/exemple/backend-mvc/public/pricing': 'nav-pricing',
            '/support': 'nav-support',
            '/exemple/backend-mvc/public/support': 'nav-support'
        };
        const activeId = navLinks[currentPath];
        if (activeId) {
            const activeLink = document.getElementById(activeId);
            if (activeLink) activeLink.classList.add('active');
        }
    </script>
    {% block extra_scripts %}{% endblock %}
</body>
</html>
