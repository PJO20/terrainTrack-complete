<!DOCTYPE html>
<html lang="fr">
<head>
  <meta charset="UTF-8">
  <title>Réinitialiser votre mot de passe - TerrainTrack</title>
  <link rel="stylesheet" href="/assets/css/style.css">
  <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.4.0/css/all.min.css">
  <style>
    body {
      background: #f8f9fb;
      min-height: 100vh;
      margin: 0;
      font-family: 'Inter', -apple-system, BlinkMacSystemFont, 'Segoe UI', Roboto, Helvetica, Arial, sans-serif;
      display: flex;
      align-items: center;
      justify-content: center;
    }
    .container {
      width: 100vw;
      height: 100vh;
      display: flex;
      flex-direction: column;
      align-items: center;
      justify-content: center;
    }
    .logo {
      background: #2346a9;
      color: #fff;
      width: 60px;
      height: 60px;
      border-radius: 50%;
      display: flex;
      align-items: center;
      justify-content: center;
      margin: 0 auto 1.5rem auto;
      font-size: 1.8rem;
    }
    .title {
      font-size: 2.2rem;
      font-weight: 700;
      color: #181c2a;
      margin-bottom: 0.5rem;
      text-align: center;
    }
    .subtitle {
      color: #555;
      font-size: 1.08rem;
      margin-bottom: 2.2rem;
      text-align: center;
      max-width: 400px;
    }
    .form-card {
      background: #fff;
      border-radius: 12px;
      box-shadow: 0 2px 16px rgba(30,40,90,0.08);
      padding: 2.2rem 2.5rem 2.5rem 2.5rem;
      min-width: 350px;
      max-width: 100%;
      display: flex;
      flex-direction: column;
      align-items: center;
    }
    .form-group {
      width: 100%;
      margin-bottom: 1.5rem;
    }
    .form-group label {
      font-size: 1rem;
      font-weight: 500;
      margin-bottom: 0.5rem;
      display: block;
      color: #222;
    }
    .form-group input[type="email"] {
      width: 100%;
      padding: 0.7rem 1rem;
      border: 1px solid #e0e0e0;
      border-radius: 6px;
      font-size: 1rem;
      background: #f7f7fa;
      outline: none;
      transition: border 0.2s;
      box-sizing: border-box;
    }
    .form-group input[type="email"]:focus {
      border-color: #2346a9;
      background: #fff;
    }
    .submit-btn {
      width: 100%;
      background: #2346a9;
      color: #fff;
      border: none;
      border-radius: 6px;
      padding: 0.9rem 0;
      font-size: 1.08rem;
      font-weight: 600;
      cursor: pointer;
      margin-bottom: 1rem;
      transition: background 0.2s;
    }
    .submit-btn:hover {
      background: #1d357a;
    }
    .submit-btn:disabled {
      background: #ccc;
      cursor: not-allowed;
    }
    .back-link {
      font-size: 0.95rem;
      color: #2346a9;
      text-decoration: none;
      font-weight: 500;
    }
    .back-link:hover {
      color: #1d357a;
      text-decoration: underline;
    }
    .alert {
      padding: 1em;
      border-radius: 6px;
      margin-bottom: 1.5rem;
      text-align: center;
      border: 1px solid;
    }
    .alert-success {
      background: #dcfce7;
      color: #166534;
      border-color: #86efac;
    }
    .alert-error {
      background: #fef2f2;
      color: #dc2626;
      border-color: #fca5a5;
    }
    .alert-info {
      background: #dbeafe;
      color: #1e40af;
      border-color: #93c5fd;
    }
    @media (max-width: 500px) {
      .form-card {
        min-width: 90vw;
        padding: 1.2rem 0.5rem 1.5rem 0.5rem;
      }
    }
  </style>
</head>
<body>
  <div class="container">
    <div class="logo">
      <i class="fa-solid fa-envelope"></i>
    </div>
        <div class="title">Mot de passe oublié</div>
    <div class="subtitle">Saisissez votre adresse e-mail et nous vous enverrons les instructions pour réinitialiser votre mot de passe.</div>

    {% if success %}
      <div class="alert alert-success">
        <i class="fa-solid fa-check-circle" style="margin-right: 0.5rem;"></i>
        {{ success }}
      </div>
    {% endif %}

    {% if error %}
      <div class="alert alert-error">
        <i class="fa-solid fa-exclamation-circle" style="margin-right: 0.5rem;"></i>
        {{ error }}
      </div>
    {% endif %}

    {% if info %}
      <div class="alert alert-info">
        <i class="fa-solid fa-info-circle" style="margin-right: 0.5rem;"></i>
        {{ info }}
      </div>
    {% endif %}

    {% if not success %}
    <form class="form-card" method="POST" id="forgotPasswordForm">
      {{ csrf_token_field|raw }}
      <div class="form-group">
        <label for="email">Adresse e-mail</label>
        <input id="email" type="email" name="email" placeholder="votre@email.com" required 
               value="{{ email ?? '' }}">
      </div>
      <button class="submit-btn" type="submit" id="submitBtn">
        <i class="fa-solid fa-paper-plane" style="margin-right: 0.5rem;"></i>
        Envoyer les instructions
      </button>
    </form>
    {% endif %}

    <a href="/login" class="back-link">
      <i class="fa-solid fa-arrow-left" style="margin-right: 0.5rem;"></i>
      Retour à la connexion
    </a>
  </div>

  <script>
    document.getElementById('forgotPasswordForm')?.addEventListener('submit', function(e) {
      const submitBtn = document.getElementById('submitBtn');
      const email = document.getElementById('email').value;
      
      if (!email) {
        e.preventDefault();
        return;
      }
      
      // Désactiver le bouton et afficher un indicateur de chargement
      submitBtn.disabled = true;
      submitBtn.innerHTML = '<i class="fa-solid fa-spinner fa-spin" style="margin-right: 0.5rem;"></i>Envoi en cours...';
      
      // Réactiver après 5 secondes au cas où il y aurait une erreur
      setTimeout(() => {
        submitBtn.disabled = false;
        submitBtn.innerHTML = '<i class="fa-solid fa-paper-plane" style="margin-right: 0.5rem;"></i>Envoyer les instructions';
      }, 5000);
    });
  </script>
</body>
</html>
